# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB Configuration
# Replace <db_password> with your actual password
MONGODB_URI=mongodb+srv://murewaajala:<db_password>@cluster0.zagqkzn.mongodb.net/fluxxx?appName=Cluster0

# CORS Configuration (comma-separated for multiple origins)
CORS_ORIGIN=http://localhost:3000,https://flux-five-wine.vercel.app

# Cloudflare TURN Configuration (for WebRTC across NAT)
# Get these from https://dash.cloudflare.com → Realtime → TURN Keys
CLOUDFLARE_TURN_KEY_ID=your_turn_key_id_here
CLOUDFLARE_TURN_API_TOKEN=your_api_token_here
